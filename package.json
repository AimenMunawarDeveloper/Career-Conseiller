{
  "name": "career-conseiller",
  "version": "1.0.0",
  "description": "Career Counselor Application",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend",
    "vercel-build": "npm run build --workspace=frontend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
